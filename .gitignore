*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/

.claude/
# Fichero de trabajo del volcado de claves de i18n
claves.txt
docs/out/

# El APK de la ficha SI va en el repositorio: lo pidio el punto 14 del brief y
# es lo que hace que un clone traiga algo instalable. El AAB no: es el artefacto
# de tienda, pesa el doble, se regenera con un comando y cada version viaja como
# asset de su GitHub Release.
docs/google_play/binarios/*.aab
